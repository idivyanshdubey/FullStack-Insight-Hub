package com.classess;

public class Opps {
	// Class
	
	    String color;//fields
	      void display() {//methods if we use private it will show error because private works within the same class 
	        System.out.println("The car color is: " + color);
	    }
	}

	


//in Java, the default keyword is used to define default methods in interfaces, not in classes.