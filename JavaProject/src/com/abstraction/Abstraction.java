package com.abstraction;//making package

  abstract class Abstraction {//making class abstract if we not make it abstract it will show error
	 abstract void draw(); // Abstract method we cannot make the object of abstract class because they are incomplete and may contain abstract method
	 //they are meant to be extended by some other classe to provide full implementation
	 //int a = 10;//can be there
	 
	}

	


//we can make interface  and abstract(both) in single program but  both have different work like we can make constructor of abstract class and can also make concrete method and fields but in interface  fields must be constant not in the case of abstract    